@model IEnumerable<CuaHangDienThoai.Models.ChiTietHoaDon>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MaHD)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MaDT)
            </th>
            <th>
                IMEI
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MaHD)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MaDT)
            </td>
            @foreach (var im in item.IMEI_DienThoais)
            {
                <td>
                    @Html.DisplayFor(im => im.)
                </td>
            }
            <td>
                <a asp-action="Edit" asp-route-id="@item.IMEI">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IMEI">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IMEI">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
